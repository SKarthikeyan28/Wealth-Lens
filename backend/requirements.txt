fastapi==0.137.1
# starlette pinned explicitly to lock the CVE-patched version fastapi pulls in
# (multipart DoS + others fixed by 1.3.1); see Phase 5.5.1 dependency audit.
starlette==1.3.1
uvicorn[standard]==0.29.0
python-json-logger==2.0.7
argon2-cffi==23.1.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
pydantic[email]==2.13.4
PyJWT==2.13.0
pyotp==2.9.0
cryptography==49.0.0
alembic==1.13.1
python-multipart==0.0.32
numpy==2.1.3
redis==5.0.4
# cvxpy-base = cvxpy without bundled solvers; clarabel (its default conic solver)
# ships ARM/manylinux wheels, so the image needs no C compiler. The bundled
# `cvxpy` distribution pulls `ecos`, which has no aarch64 wheel and fails to build.
cvxpy-base==1.5.3
clarabel==0.11.1
anthropic==0.109.2
httpx==0.27.0
